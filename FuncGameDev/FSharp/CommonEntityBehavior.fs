module CommonEntityBehavior

// move entity with id = eid
// in the direction indicated by degrees
let move eid degrees gs =
    gs;

// entity with id = eid activates
// weapon with id = wid
let useWeapon eid wid gs =
    gs;

// entity with id = eid
// collides with target entity with id = tid
let collides eid tid gs =
    gs;

// entity with id = eid dies
let die eid gs =
    gs;